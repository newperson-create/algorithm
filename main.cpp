//#include "test.hpp"
// ^异或，相同为0，不同为1

#include "test.hpp"

int main(int argc, char* argv[]) 
{

    // Timer time;
    // startGame(std::atoi(argv[0]), std::atoi(argv[1]));
    // printf("\ntime:%f\n", time.getTimerMillSecond());
}